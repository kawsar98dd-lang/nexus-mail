/* ─────────────────────────────────────────────────────────────────────────
   script.js — init() function patch
   
   খুঁজুন এই লাইনটা (line ~243):
   ─────────────────────────────────────────────────────────────────────── */

// ❌ পুরনো কোড (মুছে দিন):
if (window.innerWidth >= 769) {
    dom.sidebar.classList.add('active');
}

// ✅ নতুন কোড (এই দিয়ে replace করুন):
// Sidebar always starts hidden — user opens via hamburger button.
// (No auto-open on any screen size)

/* ─────────────────────────────────────────────────────────────────────────
   অর্থাৎ ওই if-block টা সম্পূর্ণ মুছে দিন।
   বাকি সব init() কোড একই থাকবে।
────────────────────────────────────────────────────────────────────────── */
